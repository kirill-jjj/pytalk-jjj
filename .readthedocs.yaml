version: 2

build:
  os: ubuntu-22.04
  apt_packages:
    - libpulse0
    - p7zip-full
    - libasound2
    - libssl-dev
  tools:
    python: "3.11"

sphinx:
   configuration: docs/conf.py

python:
   install:
   - method: pip
     path: .
     extra_requirements:
        - docs
